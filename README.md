# Romindr
Romindr is an iOS SwiftUI app that tracks relationship dates like anniversaries, birthdays, and partner holidays. All data is stored locally using JSON-encoded UserDefaults—no cloud or external dependencies. Features include a pastel card UI, toggled reminders, notifications, confetti, haptics, chimes, automatic sorting, and offline persistence.
